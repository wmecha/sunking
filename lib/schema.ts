/**
 * Schema initialisation — no-op.
 * The database schema is managed in Supabase (see SUPABASE_SCHEMA.sql).
 * This stub keeps existing call-sites working without modification.
 */
export async function initializeSchema(): Promise<void> {
  // Schema is pre-created in Supabase — nothing to do here.
}
